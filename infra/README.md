# Optional Research Infrastructure

This directory contains pinned launch/configuration material for tools that can improve engineering throughput or authorized data ingestion but must not become hidden dependencies of the SPX Gravity Engine.

- `context-mode/`: coding-agent context management only.
- `camofox/`: isolated authorized browser ingestion only.

Neither service can modify canonical Stage 1/2/3 outputs or place trades.
